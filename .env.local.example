# Same-origin in production: nginx serves the site and proxies /auth, /trading, /api, /CRUD to FastAPI.
NEXT_PUBLIC_API_URL=
# Dev only: where `next dev` rewrites API paths.
API_BACKEND_URL=http://localhost:8000
